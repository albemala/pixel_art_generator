name: pixel_art_generator
description: A Flutter package to generate pixel art sprites from an optional template.
version: 1.0.2
repository: https://github.com/albemala/pixel_art_generator

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
#  flutter_test:
#    sdk: flutter

  very_good_analysis: ^7.0.0

flutter:
name: pixel_art_generator_example

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  pixel_art_generator:
    path: ../

dev_dependencies:
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
